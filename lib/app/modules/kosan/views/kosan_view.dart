import 'package:flutter/material.dart';

class KosanView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Kosan')),
      body: Center(child: Text('Kosan View Content')),
    );
  }
}
